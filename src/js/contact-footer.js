/**
 * Contact Footer — Vegas Wash Co.
 * Called by main.js after DOMContentLoaded.
 */

export function init() {
  // Wire up tel/mailto links for analytics or future tracking.
  // Currently a no-op; links use native href behaviour.
}
